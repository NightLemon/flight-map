# 本机验收记录：2026-09-07

已交付可运行的本地存储、候选构建与显式发布、固定版本 API、地图/程序资料界面，以及真实 d-TPP 目录研究流程。NASR 真实机场地图与 CIFP 真实程序矢量尚未达到完整验收，原因列在本记录末尾。

## 自动检查

| 检查 | 执行结果 |
| --- | --- |
| `uv sync --all-packages --dev --locked` | 通过 |
| `uv run ruff check .` | 通过 |
| `uv run pytest` | 133 项通过 |
| `npm run lint` | 通过 |
| `npm run test:web` | 29 项通过 |
| `npm run build` | 通过；MapLibre 构建仍有大包提示 |
| `npm run test:e2e` | 2 项隔离合成资料浏览器流程通过 |
| `$env:FLIGHTMAP_LIVE = '1'; npm run test:e2e` | SEA、JFK 两项真实目录浏览器流程通过 |

真实浏览器模式要求本机 API、Web 及有效 d-TPP 发布已经运行。普通 CI 使用隔离合成样本；FAA 网络探测单独运行。远程 CI 工作流已配置，但本次没有执行 GitHub 远程任务。

最后的代码修复已验证：持久化报告损坏时拒绝读取/提升、原始腿邻接保留、显式 ICAO 搜索、覆盖状态区分、航图有效期重叠，以及地图跨经度 180° 的范围规范化。获取失败时保留最近验证通过的周期、有效期和官方更正入口，仍按实际门禁判断是否可用。

## 真实资料与页面

| 产品 | 实际结果 | 发布情况 |
| --- | --- | --- |
| d-TPP 2609 | 3,197 个无坐标机场目录项、24,231 条航图记录 | 已在本机显式提升为 Current |
| NASR 2026-09-03 | 19,411 条机场记录解析成功，0 错误；原件和报告已保存 | 精确 UTC 有效期依据缺失，阻断发布 |
| CIFP | 更新命令返回 `needs-user-action`，获取尝试及原因持久化 | 未接受协议、未获取 ZIP、无真实矢量发布 |

d-TPP 发布 ID 为 `dtpp-2609-1c493184b1bc8a79edfc6a4c`，精确区间来自 XML：`[2026-09-03T09:01:00Z, 2026-10-01T09:01:00Z)`。验证报告为输入 27,428、成功 27,416、不支持 12、错误 0。未知 DAU 类别保留原始目录记录；删除图纸保留元数据并禁用 PDF 地址。

重复 d-TPP 更新返回 `unchanged`，保持同一发布 ID。重启最新 API 后，Current 与报告恢复正确，`storage_errors` 为空；SEA 有 64 条航图目录记录，JFK 有 38 条。机场目录没有坐标，因此页面不生成地图点。

浏览器已验证搜索机场、选航图、尝试 PDF 内嵌、超时提示及官方新窗口链接。无头浏览器未确认 PDF 页面内容成功渲染，不将目录流程通过描述成 PDF 渲染已验证。现场截图保留在忽略目录 `apps/web/test-results/`。

## 生命周期验收范围

固定合成资料覆盖多输入原件、重复导入、提升失败不切换指针、重启恢复、未来/过期/撤销、身份和报告冲突、同周期更正、固定版本冲突及返回前二次检查。

`test_lifecycle_acceptance.py` 另验证新增/删除/变更分别为 1 的差异样本，以及只有原件溯源改变时语义差异为 0。完整数据目录复制到新位置后，旧路径被移走；新仓库仍能核对 Current、全部记录与报告、所有原件 SHA，并完成预览→生效→过期→历史→撤销及再次重启。这验证了 README 的完整目录备份/恢复方法。

前端合成流程覆盖版本切换、过期清理、恢复前台、网络失败、程序/分支选择、未支持腿缺口和 PDF 失败。IF/TF 测地线引擎的测试不等于正式 CIFP 程序验收。

## 尚未完成

1. NASR 的官方精确 UTC 起止依据：对应 C07a，以及 D04/D10/D11 的真实机场地图验收。
2. CIFP 官方协议处理、足够的字段与 continuation/分支/引用语义依据，以及各一条 SID、STAR、进近的独立真实预期；详见 `cifp-support.md`。
3. 可信的程序与航图标识关联规则、正式航路/程序几何与全产品真实周期演练。未确认关联保持明确提示，不按名称猜测。

上述项目在实施台账中保持 partial/blocked；本次结果不能标为批准计划的全部完成。
